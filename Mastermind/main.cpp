#include <iostream>
#include <stdlib.h>
#include <time.h>

using namespace std;

int randn(int num)
{
    return (rand() % 10);
}

char vuoicontinuare()
{
    char x
    do
    cout << "Vuoi continuare(S o N)? "
    cin >>
    while( x=!'s' || x=!'S' || x=!'n' || x=!'N')
}

int main()
{
    srand(time(NULL));
    int tentativi = 0, matrice[4][4];



    return 0;
}
